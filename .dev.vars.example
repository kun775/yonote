# 本地调试环境变量示例（复制为 .dev.vars 后填入独立的随机值）
# .dev.vars 被 .gitignore 忽略，不会提交到仓库
#
# 生产环境不要复用这些值，请用 `wrangler secret put <NAME>` 分别设置独立的随机值。
# 生成随机密钥示例：
#   openssl rand -base64 32

# 内容加密密钥（AES-GCM 派生 key 的素材）
ENCRYPTION_KEY=please-replace-with-a-local-random-value

# 认证 Cookie 签名密钥（HMAC-SHA256），必须与 ENCRYPTION_KEY 不同
AUTH_SECRET=please-replace-with-another-local-random-value

# 管理员密码的 PBKDF2 哈希
# 生成命令：node scripts/generate-password-hash.js <password>
ADMIN_PASSWORD=pbkdf2$100000$PLEASE-GENERATE-YOUR-OWN-HASH
